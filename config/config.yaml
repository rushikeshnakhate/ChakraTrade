defaults:
  - override hydra/job_logging: default
  - override hydra/hydra_logging: default

hydra:
  run:
    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ./multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}

project:
  project_name: PerformanceAnalysisForexExposure
  project_dir: D:\PerformanceAnalysisForexExposure
  data_dir: D:\PerformanceAnalysisForexExposure\data
  plugins_dir: D:\PerformanceAnalysisForexExposure\src\plugin